import React from "react";
import Table from "../../components/Table";

const ListingVehicle = (props: any) => {
  return (
    <div className="wrapper">
      <Table />
    </div>
  );
};

export default ListingVehicle;
